language: python
services:
  - postgresql
python:
  - 2.7
install:
  - pip install -r requirements/test.txt
before_script:
  - psql -c 'create database transbiz;' -U postgres
  - python manage.py migrate
script:
  - python manage.py test